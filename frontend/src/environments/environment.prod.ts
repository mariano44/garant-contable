// Configuracion de produccion. Cambia estas tres URLs por las del despliegue
// antes de compilar con `ng build --prod`.
export const environment = {
  production: true,
  apiUrl: 'https://rest.ejemplo.com/api/',
  backendUrl: 'https://rest.ejemplo.com/',
  appUrl: 'https://app.ejemplo.com/'
};
